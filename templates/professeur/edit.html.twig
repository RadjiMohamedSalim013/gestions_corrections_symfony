{% extends 'base.html.twig' %}

{% block title %}Modifier un professeur{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="mb-4 text-primary fw-bold">Modifier un professeur</h1>

    <div class="card shadow-sm border-0 p-4">
        {{ include('professeur/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>

    <div class="mt-3 d-flex gap-3 align-items-center">
        <a href="{{ path('app_professeur_index') }}" class="btn btn-secondary">
            <i class="fas fa-arrow-left me-1"></i> Retour à la liste
        </a>

        {{ include('professeur/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
