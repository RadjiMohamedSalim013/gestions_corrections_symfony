{% extends 'base.html.twig' %}

{% block title %}Modifier une épreuve{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="mb-4 text-primary fw-bold">Modifier une épreuve</h1>

    <div class="card shadow-sm border-0 p-4">
        {{ include('epreuve/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>

    <div class="mt-3 d-flex gap-3 align-items-center">
        <a href="{{ path('app_epreuve_index') }}" class="btn btn-secondary">
            <i class="fas fa-arrow-left me-1"></i> Retour à la liste
        </a>

        {{ include('epreuve/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
