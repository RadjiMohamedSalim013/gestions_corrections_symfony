{% extends 'base.html.twig' %}

{% block title %}Créer une correction{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1 class="mb-4 text-primary fw-bold">Créer une correction</h1>

    <div class="card shadow-sm border-0 p-4">
        {{ include('correction/_form.html.twig', { button_label: 'Créer' }) }}
    </div>

    <div class="mt-3">
        <a href="{{ path('app_correction_index') }}" class="btn btn-secondary">
            <i class="fas fa-arrow-left me-1"></i> Retour à la liste
        </a>
    </div>
</div>
{% endblock %}
