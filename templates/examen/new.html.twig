{% extends 'base.html.twig' %}

{% block title %}Créer un examen{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1 class="mb-4 text-primary fw-bold">Créer un examen</h1>

    <div class="card shadow-sm border-0 p-4">
        {{ include('examen/_form.html.twig', { button_label: 'Créer' }) }}
    </div>

    <div class="mt-3">
        <a href="{{ path('app_examen_index') }}" class="btn btn-secondary">
            <i class="fas fa-arrow-left me-1"></i> Retour à la liste
        </a>
    </div>
</div>
{% endblock %}
