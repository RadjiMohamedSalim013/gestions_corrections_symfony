{% extends 'base.html.twig' %}

{% block title %}Créer un établissement{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1 class="mb-4 text-primary fw-bold">Créer un établissement</h1>

    {{ include('etablissement/_form.html.twig', { button_label: 'Créer' }) }}

    <a href="{{ path('app_etablissement_index') }}" class="btn btn-secondary mt-3">
        <i class="fas fa-arrow-left me-1"></i> Retour à la liste
    </a>
</div>
{% endblock %}
